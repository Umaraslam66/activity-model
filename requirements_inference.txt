# Runtime dependencies for real Leonardo generation.
#
# Gemma 4 support is available in current vLLM, but we still pin
# transformers in the Dockerfile to keep the Gemma runtime explicit.

jsonschema==4.26.0
hf_transfer==0.1.9
vllm==0.18.1
